///
/// HybridMediaViewerComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#import "HybridMediaViewerComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridMediaViewerSpecSwift.hpp"
#import "MediaViewer-Swift-Cxx-Umbrella.hpp"

using namespace facebook;
using namespace margelo::nitro::mediaviewer;
using namespace margelo::nitro::mediaviewer::views;

/**
 * Represents the React Native View holder for the Nitro "MediaViewer" HybridView.
 */
@interface HybridMediaViewerComponent: RCTViewComponentView
@end

@implementation HybridMediaViewerComponent {
  std::shared_ptr<HybridMediaViewerSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridMediaViewerComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridMediaViewerComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridMediaViewerSpec> hybridView = MediaViewer::MediaViewerAutolinking::createMediaViewer();
    _hybridView = std::dynamic_pointer_cast<HybridMediaViewerSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  MediaViewer::HybridMediaViewerSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const react::Props::Shared&)props
            oldProps:(const react::Props::Shared&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridMediaViewerProps const>(props);
  auto& newViewProps = const_cast<HybridMediaViewerProps&>(newViewPropsConst);
  MediaViewer::HybridMediaViewerSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // isBlue: boolean
  if (newViewProps.isBlue.isDirty) {
    swiftPart.setIsBlue(newViewProps.isBlue.value);
    newViewProps.isBlue.isDirty = false;
  }
  // hasBeenCalled: boolean
  if (newViewProps.hasBeenCalled.isDirty) {
    swiftPart.setHasBeenCalled(newViewProps.hasBeenCalled.value);
    newViewProps.hasBeenCalled.isDirty = false;
  }
  // colorScheme: enum
  if (newViewProps.colorScheme.isDirty) {
    swiftPart.setColorScheme(static_cast<int>(newViewProps.colorScheme.value));
    newViewProps.colorScheme.isDirty = false;
  }
  // someCallback: function
  if (newViewProps.someCallback.isDirty) {
    swiftPart.setSomeCallback(newViewProps.someCallback.value);
    newViewProps.someCallback.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

@end
