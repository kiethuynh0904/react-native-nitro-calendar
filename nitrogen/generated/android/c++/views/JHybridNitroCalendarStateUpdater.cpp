///
/// JHybridNitroCalendarStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#include "JHybridNitroCalendarStateUpdater.hpp"
#include "views/HybridNitroCalendarComponent.hpp"
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JNISharedPtr.hpp>

namespace margelo::nitro::nitrocalendar::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridNitroCalendarState>;

void JHybridNitroCalendarStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridNitroCalendarSpec::javaobject> javaView,
                                           jni::alias_ref<react::StateWrapperImpl::javaobject> stateWrapper) {
  JHybridNitroCalendarSpec* view = javaView->cthis();
  std::shared_ptr<const react::State> state = stateWrapper->cthis()->getState();
  auto concreteState = std::dynamic_pointer_cast<const ConcreteStateData>(state);
  const HybridNitroCalendarState& data = concreteState->getData();
  const std::optional<HybridNitroCalendarProps>& maybeProps = data.getProps();
  if (!maybeProps.has_value()) {
    // Props aren't set yet!
    throw std::runtime_error("HybridNitroCalendarState's data doesn't contain any props!");
  }
  const HybridNitroCalendarProps& props = maybeProps.value();
  if (props.isBlue.isDirty) {
    view->setIsBlue(props.isBlue.value);
    // TODO: Set isDirty = false
  }
  if (props.hasBeenCalled.isDirty) {
    view->setHasBeenCalled(props.hasBeenCalled.value);
    // TODO: Set isDirty = false
  }
  if (props.colorScheme.isDirty) {
    view->setColorScheme(props.colorScheme.value);
    // TODO: Set isDirty = false
  }
  if (props.someCallback.isDirty) {
    view->setSomeCallback(props.someCallback.value);
    // TODO: Set isDirty = false
  }

  // Update hybridRef if it changed
  if (props.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = props.hybridRef.value;
    if (maybeFunc.has_value()) {
      auto shared = JNISharedPtr::make_shared_from_jni<JHybridNitroCalendarSpec>(jni::make_global(javaView));
      maybeFunc.value()(shared);
    }
    // TODO: Set isDirty = false
  }
}

} // namespace margelo::nitro::nitrocalendar::views
